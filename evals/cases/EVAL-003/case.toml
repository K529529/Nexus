case_id = "EVAL-003"
initial_repo_state = "fixture"
task = "Refactor normalize_tags in tags.py to remove the manual accumulator while preserving its exact behavior. Change only tags.py and keep all tests green."
allowed_files = ["tags.py"]
forbidden_files = ["tests/test_tags.py"]
pre_validation_command = ["pytest", "-q", "tests/test_tags.py"]
validation_command = ["pytest", "-q", "tests/test_tags.py"]
validation_timeout_seconds = 120
test_discovery_command = []
expected_behavior = "A behavior-preserving implementation refactor leaves the green tests passing."
max_reasonable_steps = 8

[[deterministic_success_conditions]]
assertion_type = "validation_passes"

[[deterministic_success_conditions]]
assertion_type = "no_unauthorized_changes"

[[deterministic_success_conditions]]
assertion_type = "no_forbidden_changes"

[[deterministic_success_conditions]]
assertion_type = "required_path_changed"
path = "tags.py"
